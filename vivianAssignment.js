//below is a program that finds the largest and smallest elements in an array.
const numbers=[2,7,9,3,8,0,1,6];
let largest=Math.max(...numbers);
let smallest=Math.min(...numbers);

//used the math methods to get the largest and smallest elements.
console.log("largest Value",largest);
console.log("Smallest Value",smallest);